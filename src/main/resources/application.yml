#spring.application.name=employee_management

server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employeeDb
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
# Mail Config
    mail:
      host: smtp.gmail.com
      port: 587
      username: goodluckhart340@gmail.com
      password: slci rvsg bugm axem
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
#JWT Config
auth:
  token:
    expirationInMils: 3600000
    jwtSecret: X7V08f8i3WF2rbit0VKrNR0IS41kCO7Vf+CgV8pIRMg=



        #========= Jwt Config ==============
        #====== 1Hr Expiration Time ========
#        auth.token.expirationInMils=3600000
#        auth.token.jwtSecret=X7V08f8i3WF2rbit0VKrNR0IS41kCO7Vf+CgV8pIRMg=
#
#        #============MAIL SENDER CONFIG============
#        spring.mail.host=smtp.gmail.com
#        spring.mail.port=587
#        spring.mail.username=${SPRING_MAIL_USERNAME}
#        spring.mail.password=${SPRING_MAIL_PASSWORD}
#        spring.mail.properties.mail.smtp.auth=true
#        spring.mail.properties.mail.smtp.starttls.enable=true